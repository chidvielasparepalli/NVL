"use client";
import{useEffect,useMemo,useRef,useState}from"react";import type{Editor}from"@tiptap/core";import{api}from"@/lib/api-client";
const languages=[["English","en-IN"],["Hindi","hi-IN"],["Telugu","te-IN"],["Tamil","ta-IN"],["Kannada","kn-IN"],["Malayalam","ml-IN"],["Marathi","mr-IN"],["Bengali","bn-IN"],["Urdu","ur-IN"],["Spanish","es-ES"],["French","fr-FR"],["German","de-DE"],["Japanese","ja-JP"]] as const;
export function VoiceInput({editor}:{editor:Editor|null}){const recognitionRef=useRef<SpeechRecognition|null>(null);const startPos=useRef(0);const endPos=useRef(0);const finalText=useRef("");const[Ctor]=useState<typeof SpeechRecognition|null>(()=>typeof window==="undefined"?null:(window.SpeechRecognition??window.webkitSpeechRecognition));const[supported,setSupported]=useState(true);const[listening,setListening]=useState(false);const[language,setLanguage]=useState("en-IN");const[translate,setTranslate]=useState(false);const[status,setStatus]=useState("Ready to dictate");useEffect(()=>{if(!Ctor)setSupported(false);},[Ctor]);
const insert=(text:string)=>{if(!editor)return;editor.chain().focus().insertContentAt({from:startPos.current,to:endPos.current},text).run();startPos.current=editor.state.selection.from-text.length;endPos.current=editor.state.selection.from;};
const start=()=>{if(!editor||!Ctor){setStatus("Use Chrome/Edge for speech input.");return;}const r=new Ctor();r.lang=language;r.continuous=true;r.interimResults=true;startPos.current=editor.state.selection.from;endPos.current=editor.state.selection.from;finalText.current="";r.onstart=()=>{setListening(true);setStatus("Listening…");};r.onresult=e=>{let all="",interim="";for(let i=0;i<e.results.length;i++){const x=e.results[i];if(x.isFinal)all+=x[0].transcript+" ";else interim+=x[0].transcript;}finalText.current=all.trim();insert((all+interim).trim()?((all+interim).trim()+" "):"");};r.onerror=e=>{setListening(false);setStatus("Mic error: "+e.error);};r.onend=async()=>{setListening(false);const raw=finalText.current.trim();if(translate&&raw){try{const d=await api("/tools/translate",{method:"POST",body:JSON.stringify({text:raw,source:language})});insert((d.translation??raw)+" ");setStatus("Translated");}catch{setStatus("Saved");}}else setStatus("Saved");};recognitionRef.current=r;r.start();};const stop=()=>recognitionRef.current?.stop();useEffect(()=>()=>recognitionRef.current?.abort(),[]);return <div className="flex flex-wrap items-center gap-2"><button disabled={!supported} onClick={listening?stop:start} className={"inline-flex items-center gap-2 rounded-full border px-3 py-2 text-xs "+(listening?"voice-active border-red-400/50 bg-red-500/15 text-red-200":"border-white/10 bg-white/[.03] text-stone-300")}><span>{listening?"■":"🎙"}</span>{listening?"Stop voice":"Voice"}</button><select value={language} onChange={e=>setLanguage(e.target.value)} className="rounded-full border border-white/10 bg-black/30 px-3 py-2 text-xs text-stone-300">{languages.map(([label,code])=><option key={code} value={code}>{label}</option>)}</select><label className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[.03] px-3 py-2 text-xs text-stone-400"><input type="checkbox" checked={translate} onChange={e=>setTranslate(e.target.checked)} className="accent-red-500"/>Translate</label><span className="text-[11px] text-stone-600">{status}</span></div>;}