"use client";
import {useState} from "react";
import {useAuth} from "@/components/session-provider";
export function AuthPanel(){const{login,register}=useAuth();const[mode,setMode]=useState<"login"|"register">("login");const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");
const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError("");try{if(mode==="login")await login(email,password);else await register(name,email,password);window.location.href="/app";}catch(err){setError(err instanceof Error?err.message:"Authentication failed");}finally{setBusy(false);}};
return <div className="w-full max-w-sm rounded-3xl border border-white/10 bg-black/30 p-5 backdrop-blur-xl"><div className="mb-5 flex rounded-full bg-white/[.04] p-1"><button type="button" onClick={()=>setMode("login")} className={"flex-1 rounded-full px-4 py-2 text-xs "+(mode==="login"?"bg-white/10 text-white":"text-stone-500")}>Sign in</button><button type="button" onClick={()=>setMode("register")} className={"flex-1 rounded-full px-4 py-2 text-xs "+(mode==="register"?"bg-white/10 text-white":"text-stone-500")}>Create account</button></div><form onSubmit={submit} className="space-y-3">{mode==="register"&&<input value={name} onChange={e=>setName(e.target.value)} placeholder="Name" required className="w-full rounded-xl border border-white/10 bg-white/[.03] px-4 py-3 text-sm outline-none"/>}<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" required className="w-full rounded-xl border border-white/10 bg-white/[.03] px-4 py-3 text-sm outline-none"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password · 8+ characters" minLength={8} required className="w-full rounded-xl border border-white/10 bg-white/[.03] px-4 py-3 text-sm outline-none"/>{error&&<div className="rounded-xl border border-red-400/20 bg-red-500/10 px-3 py-2 text-xs text-red-200">{error}</div>}<button disabled={busy} className="w-full rounded-xl bg-red-600 px-4 py-3 text-sm font-semibold text-white disabled:opacity-50">{busy?"Working…":mode==="login"?"Enter NOVAL":"Create my writing room"}</button></form></div>;}
export function SignOutButton(){const{logout}=useAuth();return <button onClick={logout} className="rounded-full border border-white/10 px-4 py-2 text-xs text-stone-300">Sign out</button>;}